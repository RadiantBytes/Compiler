#include <stdbool.h>
#include "SymTab.h"

extern void printSymTab();
extern void storeVar(char * name, int v);
;
extern int doAdd(int v1, int v2);
extern int doMult(int v1, int v2);
extern int doNegative(int v1);
extern int getVal(char * name);
